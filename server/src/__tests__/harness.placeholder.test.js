import { describe, it, expect } from "vitest";

// Temporary placeholder test used to confirm the Vitest runner boots.
// Safe to remove once real test suites exist under server/src/**.
describe("test harness", () => {
  it("boots the vitest runner", () => {
    expect(true).toBe(true);
  });
});
